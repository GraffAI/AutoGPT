{
  "name": "firststeps",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build",
    "build:watch": "tsc --build --watch --preserveWatchOutput",
    "watch": "concurrently \"npm:build:watch\" \"nodemon\""
  },
  "dependencies": {
    "shared": "1.0.0"
  },
  "devDependencies": {}
}
